## 重构要求

1. 不要对用户侧的API使用体验有影响，保持现在用户友好的API设计风格。
2.  保证测试用例作相应的更新。
3. 严格保证实现与MCP（Model Context Protocol）协议规范的高度一致。
4. com.sun.net.httpserver的选型保持不变，本项目不面向高并发场景。
5. 不新增任何依赖库，除非说明缘由并且争得了同意。（test scope的依赖无所谓，随便增加）
6. 做完任何重构之后，自行使用mvn等命令验证结果是否符合预期。

